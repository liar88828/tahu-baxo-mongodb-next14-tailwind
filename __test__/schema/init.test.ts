import { describe, expect, it } from "vitest";

describe('can test init schema', async () => {
	describe("test zod schema", () => {
		it("can validate create data", async () => {
			expect('just add').toBe('just add')
		})
	})
	
})
